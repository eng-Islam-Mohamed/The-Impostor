services:
  - type: web
    name: bara-alsalfa-multiplayer
    runtime: docker
    rootDir: server
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUBLIC_BASE_URL
        sync: false
      - key: CORS_ORIGIN
        sync: false
